PORT=5000
NODE_ENV=development

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=novabank_atm

JWT_SECRET=change_this_to_a_long_random_string_in_production
JWT_EXPIRES_IN=7d

# Comma-separated origins for CORS (optional). Leave empty to allow same-origin + Vite default.
CORS_ORIGIN=http://localhost:5173
