# Optional: call API directly (e.g. mobile on LAN). Leave empty to use Vite proxy to backend.
# VITE_API_URL=http://localhost:5000

# Optional: override proxy target when running `npm run dev` in client/
# VITE_PROXY_TARGET=http://localhost:5000
